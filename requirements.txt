PySide6==6.7.2
# google-generativeai removido: crashava em bundle Nuitka por causa de
# gRPC/Cython (cygrpc.pyd). Chamamos a REST API diretamente via requests.
pywinauto==0.6.8
pyautogui==0.9.54
Pillow==10.4.0
rapidfuzz==3.9.6
pydantic==2.8.2
keyring==25.2.1
python-dateutil==2.9.0
# Visão computacional pra auto-detectar posições dos campos do TOTVS
# (elimina calibração manual). Versão headless: sem GUI, ~40MB no bundle,
# suficiente pra matchTemplate + resize.
opencv-python-headless==4.10.0.84
numpy==1.26.4
mss==9.0.2
pygetwindow==0.0.9

# Deps transitivas fixadas em versões conhecidamente compatíveis com Nuitka 2.4.
# certifi >=2024.8 quebra no Nuitka 2.4 por causa de importlib.resources.files();
# grpc/protobuf/urllib3 novas versões também dão problemas em standalone builds.
certifi==2024.7.4
requests==2.32.3
urllib3==2.2.3

# Empacotamento (opcional em runtime; necessário só para build/build.py)
# Nuitka é MUITO melhor que PyInstaller para máquinas corporativas: gera
# binário nativo (menos falsos-positivos de AV), roda sem admin e sem
# instalação a partir de uma pasta portátil copiada por USB.
nuitka==2.4.8
zstandard==0.23.0
ordered-set==4.1.0
